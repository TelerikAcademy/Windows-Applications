<Page
    x:Class="ProgressBar.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProgressBar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ProgressBar Name="TheProgressBar"
                     Grid.Row="0"
                     Width="200"
                     Height="20"
                     />
        <ProgressRing Name="TheProgressRing"
                      Width="100"
                      Height="100"
                      Grid.Row="1"/>
        <Button Content="Change progress bars state"
                HorizontalAlignment="Center"
                Grid.Row="2"
                Click="OnChangeProgressStateButtonClick"/>
    </Grid>
</Page>
